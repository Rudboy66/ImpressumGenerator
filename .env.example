# Server-only. Never prefix this value with NEXT_PUBLIC_.
OPENAI_API_KEY=

# Optional alternative outside an automatically authenticated Vercel deployment.
AI_GATEWAY_API_KEY=

# Optional override. The current default is used when this is empty.
OPENAI_MODEL=gpt-5.6-sol
